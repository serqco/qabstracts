name: qabstracts data checks
run-name: ${{ github.actor }}
on: [push]
jobs:
  Check-Codings:
    runs-on: ubuntu-22.04
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: check-codings
        run: |
          # ls ${{ github.workspace }}
          git log --oneline -10
          # sudo apt install poppler-utils
          # pip -q install pytest
          # PYTHONPATH=script pytest -v
          python script/qabstracts.py check-codings prestudy2
          python script/qabstracts.py compare-codings prestudy2  # must yet learn about IU suffixes
          echo "Job status: ${{ job.status }}"
